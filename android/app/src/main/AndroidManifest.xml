<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity
            android:name=".widgets.WidgetQuickActionsActivity"
            android:theme="@style/AppTheme.QuickActionsDialog"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:exported="false" />

        <!-- Only ever targeted by this app's own PendingIntents/explicit
             Intents (widget taps) — no intent-filter needed or wanted. -->
        <receiver
            android:name=".WidgetActionReceiver"
            android:exported="false" />

        <receiver
            android:name=".widgets.WidgetCycleVehicleReceiver"
            android:exported="false" />

        <!-- Repaints the widgets' background-detection status dots on a timer
             (own explicit PendingIntent, no intent-filter needed). The alarm
             only exists while at least one widget is placed — see
             WidgetStatusRefresher.scheduleOrCancel(). -->
        <receiver
            android:name=".widgets.WidgetStatusRefreshReceiver"
            android:exported="false" />

        <!-- Handles the once-daily status-notification alarm (own explicit
             PendingIntent, no intent-filter match needed for that) AND
             BOOT_COMPLETED (a protected system broadcast — safe to declare
             even though exported=false, since only the system can send it).
             Manifest-registered rather than dynamic specifically so it keeps
             receiving BOOT_COMPLETED across reboots regardless of whether
             any Service happens to be running. -->
        <receiver
            android:name=".DailyStatusReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Restarts ParkingForegroundService after the two events that kill the
             process with no user action (reboot, APK update). Without this,
             background BT/GPS detection stayed dead until the app was next
             opened by hand — every setReasonActive() caller is a @PluginMethod,
             reachable only from live JS. Both actions are protected system
             broadcasts, so exported="false" is correct. -->
        <receiver
            android:name=".ServiceRestartReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

        <service
            android:name=".ParkingForegroundService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice|specialUse|location">
            <!-- Android 14+ requires BLUETOOTH_CONNECT to already be GRANTED
                 (not just declared) before starting a connectedDevice-typed
                 foreground service. That permission is only requested lazily
                 (linking a vehicle's BT device in settings), so a fresh
                 install has it started via the "parking" reason (any saved
                 parking, GPS-only) long before Bluetooth is ever touched.
                 specialUse has no such runtime-permission prerequisite —
                 ParkingForegroundService.kt picks it whenever
                 BLUETOOTH_CONNECT isn't granted yet, so starting the service
                 never crashes regardless of which reason triggered it. -->
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Keeps Bluetooth and GPS parking detection active in the background" />
        </service>

        <receiver
            android:name=".widgets.ActiveParkingWidgetProvider"
            android:exported="true"
            android:label="חניה פעילה">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_active_parking_info" />
        </receiver>

        <receiver
            android:name=".widgets.QuickSaveWidgetProvider"
            android:exported="true"
            android:label="שמירה מהירה">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_quick_save_info" />
        </receiver>

        <receiver
            android:name=".widgets.MiniMapWidgetProvider"
            android:exported="true"
            android:label="מפה מוקטנת">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_mini_map_info" />
        </receiver>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <!-- Required on Android 14+ for the service's `location` foreground-service
         type, which is itself what allows ParkingForegroundService's
         LocationManager watch to keep receiving updates while the app is
         backgrounded (Android 10+ blocks background location otherwise — see
         CLAUDE.md "Background location needs a location-typed foreground
         service"). Without this the GPS drive-away detection only ever worked
         while the app was actually on screen. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Package visibility (Android 11+): without these, packageManager
         .resolveActivity() cannot SEE these OEM settings apps at all, so
         OemSettingsIntents.autostartAvailable()/oemBatteryAvailable() would
         always report false and the setup guide would wrongly tell the user
         their device has no Autostart screen. Launching still works via
         try/catch regardless — this is purely so the guide can tell the truth
         about what's available BEFORE the user taps. Deliberately an explicit
         list rather than QUERY_ALL_PACKAGES, which is a restricted permission
         requiring Play Store justification and is wildly disproportionate for
         this. -->
    <queries>
        <!-- Xiaomi / Redmi / POCO (MIUI + HyperOS) -->
        <package android:name="com.miui.securitycenter" />
        <package android:name="com.miui.powerkeeper" />
        <!-- Huawei / Honor -->
        <package android:name="com.huawei.systemmanager" />
        <!-- Oppo / Realme (ColorOS) -->
        <package android:name="com.coloros.safecenter" />
        <package android:name="com.coloros.oppoguardelf" />
        <!-- Vivo -->
        <package android:name="com.vivo.permissionmanager" />
        <!-- Samsung -->
        <package android:name="com.samsung.android.lool" />
        <!-- Letv / Asus -->
        <package android:name="com.letv.android.letvsafe" />
        <package android:name="com.asus.mobilemanager" />
    </queries>
</manifest>
