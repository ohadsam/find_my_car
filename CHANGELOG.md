# CHANGELOG — FindMyCar

All notable changes to this project are documented here.  
Format: `[version] YYYY-MM-DD`

---

## [1.36.1] — 2026-09-10

### 🔍 חקירה פתוחה — לא התקבלה התראה על חיבור/ניתוק Bluetooth

- **הדיווח**: משתמש התחבר והתנתק מ-Bluetooth של הרכב (ונסע פעמיים) ולא קיבל
  שום התראה בתריס שמציעה סיום או התחלת חניה.
- **מה נמצא**: מניתוח יומן האבחון על פני שני דיווחים (בהפרש שעות) — המקלט
  (`BroadcastReceiver`) שאמור לקלוט את אירועי ה-Bluetooth הגולמיים של המערכת
  היה רשום ופעיל ברציפות לאורך כל החלון (הוכח מרישומי "פעימת לב"), אבל
  **אף פעם** לא נרשמה אצלו קליטה של אירוע Bluetooth גולמי — לא אחד. זה מצביע
  על כך שמערכת ההפעלה עצמה מעולם לא מסרה לאפליקציה שום אירוע חיבור/ניתוק
  Bluetooth בחלון הזמן הזה — הבעיה היא "מעל" כל לוגיקת ההחלטות של האפליקציה,
  לא בתוכה.
- **מה נוסף** (אבחון בלבד, לא תיקון — עדיין אין מספיק מידע כדי לדעת מה בדיוק
  לתקן): רישום נוסף שמאזין גם לשינויי מצב של מתג ה-Bluetooth עצמו (הדלקה/
  כיבוי), כדי שבדיווח הבא נוכל לדעת בוודאות אם אירועי Bluetooth בכלל מגיעים
  לאפליקציה או שהם חסומים לגמרי.

## [1.36.0] — 2026-09-10

### 🐛 תיקון באג

#### רישומי "פעימת לב" לא נרשמו בקצב קבוע במצב חיסכון בסוללה

- **הבעיה**: המשתמש דיווח שרישומי "פעימת לב" של שירות הרקע ביומן האבחון לא
  נרשמו כל 5 דקות כפי שציפינו — בפועל הפער בין רישומים הגיע עד ל-20 דקות
  ויותר, וגדל ככל שהמכשיר נשאר במצב מנוחה (Doze) זמן ארוך יותר. הסיבה:
  המנגנון המקורי הסתמך על טיימר פשוט (`Handler`) שאין לו יכולת להעיר את
  המעבד ממצב שינה — הוא פשוט "חיכה" שמשהו אחר יעיר את המעבד.
- **הפתרון**: הוחלף במנגנון `AlarmManager` שמותאם למצב Doze של אנדרואיד
  ומסוגל להעיר את המכשיר במועד המתוכנן גם כשהוא במנוחה עמוקה.

### ✨ תכונה חדשה

#### התראה יומית על סטטוס חניה

- פעם ביום (בסביבות השעה 9:00 בבוקר) האפליקציה (באנדרואיד) שולחת התראה
  שמסכמת אילו רכבים חונים כרגע ואיפה — כדי לוודא בקלות שהאפליקציה וזיהוי
  הרקע שלה עדיין פעילים, בלי לפתוח את האפליקציה או לבדוק את יומן האבחון.
- ניתנת להפעלה/כיבוי גלובלית בהגדרות ("📋 התראת סטטוס יומית"), וגם בנפרד
  לכל רכב (בעריכת הרכב, "📋 כלול בהתראת הסטטוס היומית") — כברירת מחדל
  ההתראה כבויה גלובלית, אך כל רכב כלול בה מרגע שמפעילים אותה.

## [1.35.0] — 2026-09-09

### ✨ תכונה חדשה

#### יומן אבחון מאוחד: תחילית מקור, זמן רישום כפול ופעימות לב

- **הבעיה**: יומן האבחון המאוחד כבר איחד רישומים מהאפליקציה ומהתהליכים
  הנייטיביים, אבל שורה בודדת לא ציינה איזה תהליך בפועל כתב אותה, ולא היה
  אפשר להבחין בין "זה קרה עכשיו" לבין "זה קרה מזמן ורק עכשיו נרשם ליומן"
  (רלוונטי לרישומים נייטיביים שממתינים עד שהאפליקציה נפתחת מחדש). בנוסף,
  שקט ממושך ביומן היה דו-משמעי — אי אפשר היה לדעת בוודאות אם באמת לא קרה
  כלום (למשל הרכב עמד במקום) או שאחד התהליכים ברקע פשוט הפסיק לרוץ.
- **הפתרון**:
  - כל שורה ביומן האבחון מציגה עכשיו תחילית `[מקור]` — `[WEB]` לרישומים
    חיים מתוך האפליקציה, או תג נייטיבי ספציפי (למשל `[FMC-FgService]`,
    `[FMC-BtPlugin]`) לרישומים שהגיעו מהצד הנייטיבי — כדי לדעת מיד איזה
    תהליך כתב כל שורה.
  - כל רישום ביומן שומר עכשיו גם את הזמן שבו האירוע עצמו קרה וגם את הזמן
    שבו הוא נרשם בפועל ליומן; כשיש פער משמעותי בין השניים (רישום נייטיבי
    שהמתין להיפתח ביומן), השורה מציגה גם "נרשם בפועל ב-..." עם הזמן האמיתי.
  - נוספו רישומי "פעימת לב" — הן מהאפליקציה (JS) והן משירות הרקע הנייטיבי —
    כל כמה דקות, כדי שאפשר יהיה לראות בוודאות שהם ממשיכים לרוץ ברקע כרגיל,
    ולא רק בנקודות ההתחלה/סיום.

## [1.34.0] — 2026-09-09

### ✨ תכונה חדשה

#### יומן אבחון: תקשורת נייטיב↔אפליקציה ("BRIDGE")

- **הבעיה**: גם עם יומן "שירות רקע" (1.33.0), עדיין לא הייתה דרך לדעת
  בוודאות אם הודעה ספציפית שנשלחה מהצד הנייטיבי לאפליקציה (או להפך) באמת
  הגיעה ליעדה — למשל אם הצד הנייטיבי שלח בהצלחה אירוע, אבל ה-WebView נסגר
  בדיוק ברגע שבין השליחה לקבלה, לא הייתה שום עדות שההודעה בכלל נשלחה.
- **הפתרון**: כל קריאה מהאפליקציה לצד הנייטיבי, וכל הודעה שהצד הנייטיבי
  שולח לאפליקציה, נרשמות עכשיו ביומן האבחון תחת קטגוריה חדשה — "תקשורת
  נייטיב↔אפליקציה". כך אפשר להצליב בין רישום "ההודעה נשלחה" לרישום "ההודעה
  התקבלה וטופלה" ולוודא בוודאות שאין הודעות שאבדות בדרך

## [1.33.0] — 2026-09-09

### ✨ תכונה חדשה

#### יומן אבחון חדש: "שירות רקע (נייטיב)"

- **הבעיה**: אירועי Bluetooth/GPS שכבר קיבלו החלטה (חיה או ממתינה לביצוע)
  נרשמו ביומן, אבל לא הייתה שום דרך לדעת מתוך האפליקציה אם שירות הרקע עצמו
  — זה שאמור לשמור על האזנה ל-Bluetooth ומעקב GPS פעילים כשהמסך כבוי —
  באמת רץ ברציפות, או מתי בדיוק. שקט ביומן היה יכול להעיד גם על "הכל
  תקין, פשוט לא קרה כלום" וגם על "השירות מת" — ואי אפשר היה להבדיל בין
  השניים בלי adb ומכשיר מחובר.
- **הפתרון**: קטגוריה חדשה ביומן האבחון — "שירות רקע (נייטיב)" — מציגה
  עכשיו מתי השירות עלה/ירד, מתי מעקב ה-GPS התחיל/הופסק (וכל סיבת כישלון —
  חסרת הרשאה, ללא ספק מיקום זמין וכו'), ומתי בפועל התקבלה שידור Bluetooth
  גולמי מהמערכת — עם הזמן האמיתי שבו כל אירוע קרה, גם אם הוא קרה בזמן
  שהאפליקציה הייתה סגורה לגמרי. כך אפשר לדעת בוודאות מה קרה ומתי, ישירות
  מתוך יומן האבחון הקיים

## [1.32.0] — 2026-09-09

### 🐛 Bug Fixes

#### הווידג'טים נשארו תקועים על "מיקום נשמר" במקום הכתובת האמיתית

- **אובחן**: שמירת/החלפת/עדכון חניה מסנכרנת מיד לווידג'טים ברגע השמירה —
  אבל באותו רגע הכתובת עדיין לא נקבעה (חיפוש הכתובת מתבצע ברקע ולוקח כמה
  שניות). כשהכתובת בסופו של דבר מתקבלת, הקוד עדכן רק את התצוגה בתוך
  האפליקציה עצמה — ולא סנכרן שוב לווידג'טים. התוצאה: הווידג'ט "חניה
  פעילה" (וטקסט הכתובת בווידג'ט "מפה מוקטנת") נשארו תקועים על השם הכללי
  "מיקום נשמר" לצמיתות, גם כשבתוך האפליקציה הכתובת כבר מוצגת נכון. הפינה
  על המפה עצמה (בווידג'ט "מפה מוקטנת") לא הושפעה, כי היא מבוססת על
  קואורדינטות ולא על הכתובת המילולית.
- **התיקון**: ברגע שהכתובת מתקבלת, מתבצע עכשיו סנכרון נוסף לווידג'טים
  (ולנוטיפיקציית "חניה פעילה" בצד הנייטיבי) — לא רק לתצוגה בתוך האפליקציה

#### חניה שהסתיימה אוטומטית ברקע לרכב לא-פעיל לא עדכנה את הווידג'טים

- **אובחן**: כשחיבור Bluetooth מסיים אוטומטית חניה של רכב שאינו הרכב
  שנבחר כרגע באפליקציה, הנתונים נשמרו נכון בהיסטוריה — אבל הסנכרון
  לווידג'טים דילג על המקרה הזה, כך שהווידג'ט המשיך להראות את הרכב הזה
  כחונה למרות שהחניה כבר הסתיימה
- **התיקון**: הסנכרון לווידג'טים מתבצע עכשיו תמיד, גם כשהחניה שהסתיימה
  שייכת לרכב שאינו הרכב הפעיל כרגע

## [1.31.0] — 2026-09-09

### 🐛 Bug Fixes

#### הצעת סיום חניה לפי GPS לא הופיעה כלל ברקע (לא בהתראה, לא באפליקציה)

- **אובחן**: מיומן האבחון עלה שהשלב הנייטיבי (`GPS-SHADOW`) זיהה נכון שהרכב
  זז מהחניה גם כשהאפליקציה הייתה ברקע — אבל שום הצעה חיה ושום התראה לא
  הופיעו באותו רגע. ההצעה קפצה רק בפתיחה הבאה של האפליקציה, לפעמים הרבה זמן
  אחרי שהרכב כבר לא היה במקום. הסיבה: `ParkingForegroundService`'s
  `maybeRecordPendingGpsSuggestion()` בדק אם ה-WebView "נגיש"
  (`MainActivity.getActiveWebView() != null`) כדי להחליט אם לסמוך על הטיפול
  החי בצד ה-JS — אבל הבדיקה הזו נשארת אמת לאורך כל חיי ה-Activity, כולל כל
  הזמן שהאפליקציה ברקע (מסך כבוי, לא נסגרה) ולא רק כשהיא באמת גלויה על
  המסך. בזמן שהאפליקציה ברקע, Android חוסם בפועל את
  `navigator.geolocation.watchPosition()` (ה-API שדרכו ה-JS מזהה תזוזה),
  גם כש-`KeepRunning` שומר על ה-JS עצמו רץ — כך שהתנאי הישן חשב שה-JS
  יטפל בזה בזמן אמת, בעוד בפועל שום דבר לא טיפל בזה עד לפתיחה הבאה
- **התיקון**: `MainActivity` עוקב עכשיו אחרי מצב חזית אמיתי
  (`onResume`/`onPause`), נפרד מבדיקת קיום ה-WebView. `maybeRecordPendingGpsSuggestion()`
  משתמש עכשיו בבדיקה הזו — כך שברגע שהאפליקציה עוברת לרקע, ההצעה נרשמת
  והתראת מערכת מוצגת מיד, ולא רק כשחוזרים לאפליקציה

## [1.30.0] — 2026-09-08

### ✨ תכונה חדשה

#### תמיכה במספר רכבים חונים בו-זמנית בווידג'טים

- **הבעיה**: כשלשני רכבים יש חניה פעילה בו-זמנית, הווידג'טים "חניה פעילה" ו-
  "מפה מוקטנת" הציגו רק רכב אחד, בלי שום דרך לבחור איזה מהם רואים.
- **הפתרון**:
  - בגודל הרגיל (ברירת המחדל) של הווידג'ט מופיע כפתור 🔁 קטן שמאפשר לעבור
    בין הרכבים החונים — הבחירה נשמרת בנפרד לכל מופע של הווידג'ט (אם יש כמה
    מופעים מונחים על המסך).
  - אם מגדילים את הווידג'ט (גרירה להגדלה) מספיק, שני הווידג'טים מציגים את
    שני הרכבים החונים הראשונים בו-זמנית — שורת כתובת שנייה בווידג'ט "חניה
    פעילה", ומפה שנייה מוערמת בווידג'ט "מפה מוקטנת" — והכפתור 🔁 נעלם, כי
    אין יותר בין מה לבחור.
  - עם רכב חונה אחד בלבד, שני הווידג'טים נראים בדיוק כמו קודם — ללא שינוי
    ויזואלי.

## [1.29.0] — 2026-09-08

### 🐛 Bug Fixes

#### תפריט הווידג'טים ("⋮") פתח את האפליקציה אחרי ביצוע פעולה

- **אובחן**: `WidgetQuickActionsActivity` (חלון הפעולות הקטן שנפתח מ"⋮") לא
  הצהיר על `taskAffinity` משלו, כך שברירת המחדל שלו הייתה זהה לזו של
  `MainActivity`. בשילוב עם `FLAG_ACTIVITY_NEW_TASK` בכוונת ההפעלה, זה גרם
  ל-Android "להעלות" משימה קיימת של האפליקציה (אם הייתה כזו ברקע — כלומר כל
  פעם שהמשתמש כבר פתח את האפליקציה פעם אחת) ברגע שנפתח החלון הקטן, ולחשוף
  אותה מייד עם סגירת החלון — נראה בדיוק כאילו האפליקציה "נפתחה" אחרי לחיצה
  על פעולה, למרות שהפעולה עצמה בוצעה כראוי ברקע
- **התיקון**: `android:taskAffinity=""` מבודד את החלון למשימה נפרדת תמיד,
  כך שסגירתו חוזרת בדיוק למה שהיה על המסך לפני כן

### ✨ שיפור

- פעולות ווידג'ט (שמור/החלף/סיים) מציגות עכשיו נוטיפיקציית מערכת שמאשרת מה
  קרה ולאיזה רכב — לא רק הודעת "מבצע…" הרגעית בזמן הלחיצה. זה חל גם על
  פעולות שמבוצעות אחרי שהאפליקציה נפתחת מחדש בעקבות לחיצה שנרשמה כשהאפליקציה
  הייתה סגורה לגמרי, כי שתי הדרכים עוברות דרך אותו קוד אמיתי

## [1.28.0] — 2026-09-08

### 🔧 Technical — שלב 9: נוטיפיקציית "חניה פעילה" עוברת לנייטיב

הנוטיפיקציה המתמשכת שמציגה את כתובת החניה הפעילה ("FindMyCar — חניה פעילה
🅿️") הייתה עד עכשיו תלויה לגמרי ב-JS/Service Worker — משמע שיכלה "להיתקע"
עם כתובת ישנה, או לא להיעלם כשהחניה מסתיימת, בדיוק ברגע שה-WebView נגרס
ברקע (המקרה שכל המיגרציה הזו נועדה לפתור).

- הנוטיפיקציה נשלחת ומתעדכנת עכשיו ישירות מהצד הנייטיבי, מאותה נקודה
  שכל שינוי אמיתי במצב החניה כבר עובר דרכה — כולל כשהפעולה מגיעה מהשמעה
  מחדש של Bluetooth/GPS אחרי שהאפליקציה הייתה סגורה לגמרי
- ה-PWA (בדפדפן) ממשיך לעבוד בדיוק כפי שהיה — הנתיב הנייטיבי הוא מקביל,
  לא תוספת, כך שלא מוצגות שתי נוטיפיקציות בו-זמנית
- `release-checklist` עודכן לוודא שאין כפילות ושהערוץ הנייטיבי החדש שקט
  (לא מתריע), כמו בגרסת ה-JS המקורית

## [1.27.0] — 2026-09-08

### 🔧 Technical — שלב 8 (סיום התוכנית המקורית) — ווידג'טים headless גם כשהאפליקציה סגורה לגמרי

זהו השלב האחרון בתוכנית המקורית של העברת זיהוי Bluetooth/GPS לקוד נייטיבי.
עד עכשיו, לחיצה על פעולת ווידג'ט (שמור/החלף/סיים) כשהאפליקציה הייתה סגורה
לגמרי (לא רק ברקע) פתחה את האפליקציה — לא headless. השלב הזה סוגר גם את
הפער הזה.

- לחיצה על פעולת ווידג'ט כשהאפליקציה סגורה לגמרי כבר לא פותחת את
  האפליקציה — מוצגת הודעה מיידית ("יבוצע כשהאפליקציה תיפתח מחדש") והפעולה
  מבוצעת אוטומטית בפתיחה הבאה, דרך אותו קוד אמיתי שמבצע פעולות ווידג'ט
  בזמן שהאפליקציה כן פתוחה
- כפתור "ניהול רכבים" בתפריט הווידג'ט ממשיך לפתוח את האפליקציה כרגיל —
  הוא היחיד שבאמת צריך ממשק
- תוקנה גם השמטה מגרסה קודמת — קטגוריית **WIDGET** ביומן האבחון לא הופיעה
  בתפריט הסינון

**זהו סיום 8 השלבים המתוכננים** להעברת זיהוי Bluetooth/GPS לקוד נייטיבי —
Bluetooth, GPS, וכעת גם פעולות ווידג'ט עובדים באופן אמין לגמרי, גם כשהמסך
כבוי או כשהאפליקציה נסגרת לחלוטין ברקע, בדומה לאופן שבו אפליקציות כמו Waze
עובדות.

## [1.26.0] — 2026-09-08

### 🔧 Technical — שלב 7 (סיום) בהפיכת GPS לקוד נייטיבי לגמרי

השלמת "הפיכת GPS לחי" — בניגוד ל-Bluetooth, כאן השינוי נשלח בבת אחת (לא
בשני שלבים נפרדים), כי ל-GPS יש רק סוג החלטה אחד: "הצע לסיים חניה" — שתמיד
דורש אישור ידני ואף פעם לא מבוצע אוטומטית, כמו ה-AutoEnd/AutoStart של
Bluetooth. אין "פעולה אוטומטית" מקבילה שצריך שלב נפרד להפעיל.

- כשהאפליקציה ברקע לגמרי (ה-WebView לא זמין) וחוצה סף GPS (מהירות או
  מרחק), הצד הנייטיבי שומר את זה ומציג התראה
- בפתיחת האפליקציה הבאה, ההצעה מוצגת **כאותו חלון אישור בדיוק** שהיה מוצג
  אם האפליקציה הייתה פתוחה — ולא כפעולה אוטומטית. המשתמש עדיין צריך לאשר
  שהחניה הסתיימה
- אם הרכב הפעיל השתנה בינתיים, או שהחניה כבר הסתיימה בדרך אחרת — ההצעה
  פשוט לא מוצגת, בלי טעות
- חילוץ `BackgroundAlertNotifier` משותף כדי שהתראות הרקע של Bluetooth
  ו-GPS ישתמשו באותו קוד, במקום שני מימושים נפרדים שעלולים לסטות
- תוקנה גם השמטה מגרסה קודמת — קטגוריית **BT-PENDING** ביומן האבחון לא
  הופיעה בתפריט הסינון

## [1.25.0] — 2026-09-08

### 🔧 Technical — שלב 6 (סיום) בהפיכת Bluetooth לקוד נייטיבי לגמרי

השלמת "הפיכת Bluetooth לחי" — **מהשלב הזה יש שינוי אמיתי בהתנהגות**: אירוע
Bluetooth שקרה כשה-WebView לא היה זמין בכלל כבר לא נשאר רק התראה — הוא
מבוצע בפועל בפתיחה הבאה של האפליקציה.

- בפתיחת האפליקציה, כל פעולה שנשמרה על ידי הצד הנייטיבי (ראה 1.24.0) עוברת
  **דרך אותם פונקציות JS אמיתיות שמטפלות באירוע Bluetooth חי** — לא מימוש
  כפול. זה אומר שההחלטה מחושבת מחדש לפי ההגדרות העדכניות ברגע הפתיחה, לא
  לפי מה שהיה נכון כשהאירוע קרה בפועל (למשל אם המשתמש כיבה "סיום אוטומטי"
  בינתיים)
- שמירת חניה אוטומטית (ניתוק Bluetooth) משתמשת במיקום GPS חי בפתיחת
  האפליקציה, לא במיקום שנשמר ברקע — מדויק יותר
- הפעולות מבוצעות אחת אחרי השנייה (לא בו-זמנית), בדיוק כמו שאירועי
  Bluetooth אמיתיים תמיד מגיעים אחד בכל פעם
- אם רכב כבר במצב עקבי (למשל המשתמש כבר סיים את החניה ידנית) — הפעולה
  פשוט לא עושה כלום, בדיוק כמו שקורה גם באירוע חי רגיל
- `release-checklist` עודכן לוודא שההשמעה מחדש עוברת דרך הקוד האמיתי,
  לא לוגיקה כפולה, ושאין חסימה של פתיחת האפליקציה בזמן ההשמעה

## [1.24.0] — 2026-09-07

### 🔧 Technical — שלב 5 מתוך העברת Bluetooth לקוד נייטיבי (חצי ראשון)

תחילת "הפיכת Bluetooth לחי" — עדיין **ללא שינוי בנתוני חניה בפועל**, רק
תשתית לשמירת המידע כשה-WebView לא זמין בכלל.

- כשמתקבל אירוע Bluetooth אמיתי (חיבור/ניתוק) וה-WebView **זמין**, שום דבר
  לא משתנה — הזרימה הרגילה (שכבר עובדת) ממשיכה לטפל בזה לבד
- רק כשה-WebView **לא זמין** (התהליך נשמר ברקע אבל ה-Activity/WebView
  נגרסו) — המקרה שכל המיגרציה הזו נועדה לפתור — הקוד הנייטיבי מחשב את
  ההחלטה בפועל (לא רק ב"מצב צל"), שומר אותה (רכב, פעולה, מיקום אם רלוונטי,
  זמן) ומציג התראה ישירה מהצד הנייטיבי, כדי שהמידע לא ילך לאיבוד
- ההחלטה הנשמרת **עדיין לא מבוצעת בפועל** — האפליקציה רק מציגה אותה ביומן
  האבחון (קטגוריה חדשה **BT-PENDING**) בפתיחה הבאה, כדי לוודא שהמנגנון
  עובד נכון לפני שסומכים עליו לבצע שינוי אמיתי בנתוני החניה — זה יגיע
  בשלב הבא
- `release-checklist` עודכן לוודא שהמנגנון החדש לא "מכפיל" פעולה (לא רושם
  כשה-WebView בכל זאת זמין) ושהוא לא נוגע בנתוני חניה אמיתיים בשלב זה

## [1.23.0] — 2026-09-07

### 🔧 Technical — שלב 4 מתוך העברת זיהוי GPS לקוד נייטיבי (מצב צל)

חיבור `GpsDecisionEngine` (שלב 3) לצפייה אמיתית במיקום — **עדיין ללא כל פעולה
בפועל מהצד הנייטיבי**, בדיוק כמו שלב 2 של ה-Bluetooth.

- `ParkingForegroundService` מריץ עכשיו צפייה אמיתית במיקום (`LocationManager`
  רגיל, בלי תלות ב-Google Play Services) בזמן שיש חניה פעילה בלבד — מתחילה
  ומפסיקה בדיוק כשחניה מתחילה/מסתיימת, לא בכל עדכון מצב
- כל עדכון מיקום מוזן ל-`GpsDecisionEngine`, והתוצאה נרשמת ל-Logcat ולקטגוריה
  חדשה **GPS-SHADOW** ביומן האבחון באפליקציה — להשוואה מול ההחלטה האמיתית של
  ה-JS, בלי adb
- `js/widget-bridge.js` מסנכרן כעת גם את הגדרת "סיום חניה אוטומטי לפי GPS"
  הגלובלית לצד השדות הקיימים
- כל השרשור עטוף ב-try/catch נפרד משלו, כדי שתקלה בחישוב הצל לעולם לא תשפיע
  על שירות הרקע או על טיפול ה-Bluetooth
- `release-checklist` עודכן לבדוק שגם מצב הצל של GPS נשאר "צל" בלבד

## [1.22.0] — 2026-09-07

### 🔧 Technical — שלב 3 מתוך העברת זיהוי GPS לקוד נייטיבי (מנוע בלבד)

תחילת המקבילה של מיגרציית ה-Bluetooth (שלבים 1-2) עבור GPS: `GpsDecisionEngine`
חדש ב-`core` מפרט מחדש (ב-Kotlin טהור, ללא תלות ב-Android) את לוגיקת ההחלטה של
`#checkGpsSpeed`/`#checkGpsDistance`/`#suggestGpsEnd` — מהירות מתמשכת מעל סף
ומרחק מנקודת החניה. **שלב זה הוא מנוע בלבד — עדיין לא מחובר לצפייה אמיתית
במיקום, ואין שום שינוי בהתנהגות בפועל.**

- בניגוד ל-`BtDecisionEngine` (פונקציה טהורה של קלט חיצוני), החלטת GPS תלויה
  במצב לאורך זמן (`speedSince`/`endSuggested`) — לכן `GpsDecisionEngine` מעוצב
  כ"reducer" מפורש: מקבל מצב וקלט, מחזיר מצב חדש והחלטה, בלי מצב פנימי משלו
  ובלי קריאה לשעון עצמו (הזמן הנוכחי מגיע כפרמטר) — כדי שהבדיקות יהיו דטרמיניסטיות
  לחלוטין
- `GpsMath` חדש מפרט מחדש את חישוב המרחק (Haversine) מ-`js/utils.js`
- שני הקבצים מכוסים בבדיקות JUnit (ללא Robolectric — טהורים לחלוטין)
- `release-checklist` עודכן לבדוק גם את טוהר מנוע ה-GPS

## [1.21.0] — 2026-09-07

### 🔧 Technical — שלב 2 מתוך העברת זיהוי Bluetooth/GPS לקוד נייטיבי (מצב צל)

חיבור `BtDecisionEngine` (שלב 1) לטיפול האמיתי באירועי Bluetooth — **עדיין ללא כל
פעולה בפועל מהצד הנייטיבי**. המטרה: לבנות ביטחון שההחלטה הנייטיבית מסכימה עם
ההחלטה שה-JS מקבל בפועל, לפני שסומכים על הנייטיבי בשלב הבא.

- בכל חיבור/ניתוק Bluetooth אמיתי, `BluetoothClassicPlugin` שולח כרגיל את האירוע
  ל-JS (ללא שינוי), ובנפרד מחשב מה `BtDecisionEngine` *היה* מחליט לפי אותה רשימת
  רכבים, ורושם את התוצאה — ל-Logcat ולאירוע `btShadowDecision` חדש
- יומן האבחון (הגדרות → "יומן אבחון") מקבל קטגוריה חדשה **BT-SHADOW** שמציגה את
  ההחלטה הנייטיבית לצד קטגוריית ה-BT הרגילה, כדי שאפשר יהיה להשוות בין השתיים
  ישירות מתוך האפליקציה, בלי adb
- `js/widget-bridge.js` מסנכרן כעת גם `hasParking` לכל רכב בנפרד (לא רק לרכב
  הפעיל) — נדרש כדי שההחלטה הנייטיבית תדע איזה רכב באמת בחניה פעילה
- `BtShadowFormatter` חדש ב-`core` (מכוסה בבדיקות JUnit) מעצב את ההחלטות לתקציר
  קריא; כל השרשור עטוף ב-try/catch כדי שתקלה בחישוב הצל לעולם לא תשפיע על הטיפול
  האמיתי באירוע ה-Bluetooth

## [1.20.0] — 2026-09-07

### 🔧 Technical — שלב 1 מתוך העברת זיהוי Bluetooth/GPS לקוד נייטיבי

תחילת מיגרציה בשלבים של קבלת ההחלטות (לא כל לוגיקת החניה — תמונות/הקלטה/תיאור/היסטוריה
נשארים ב-JS) מה-WebView לקוד Kotlin נייטיבי, כדי שזיהוי הרקע יעבוד באופן דומה ל-Waze:
הלוגיקה עצמה תרוץ בתוך שירות הרקע ולא תהיה תלויה בשרידות ה-WebView/Activity ברקע.
**שלב זה תשתיתי בלבד — אין עדיין שינוי בהתנהגות בפועל.**

- `android/.../core/` — חבילת Kotlin טהורה (ללא תלות ב-Android/org.json):
  `NativeVehicle`, `BtDecisionEngine` — פורט מדויק של ענפי ההחלטה ב-
  `#onBtConnected`/`#onBtDisconnected` (js/app.js), **לא** עדיין מחובר לטיפול
  האמיתי באירועי Bluetooth
- `VehicleJsonParser` — קורא את רשימת הרכבים שכבר מסונכרנת ל-SharedPreferences;
  `js/widget-bridge.js` מסנכרן כעת גם את השדות `bluetoothDevice`/`bluetoothAutoEnd`/
  `bluetoothAutoStart`/`bluetoothStartPopup`, לא רק id/name/icon
- נוסף Robolectric (`android/app/build.gradle`) כדי שבדיקות JVM יוכלו להריץ קוד
  שנוגע ב-org.json/SharedPreferences/Context בלי אמולטור — הסביבה הזו חסרת Android
  SDK מקומי, כך שבדיקות נייטיביות רצות רק ב-CI
- `.github/workflows/build-android.yml` מריץ כעת `./gradlew testDebugUnitTest`
  **לפני** `assembleDebug` — בדיקה נייטיבית שנכשלת תעצור את בניית ה-APK
- `release-checklist` עודכן לבדוק ששלב הבדיקות הנייטיבי קיים ומצליח בכל שחרור,
  ושה-`core` package נשאר טהור (ללא תלות ב-Android) כדי להישאר ניתן לבדיקה מהירה

## [1.19.0] — 2026-09-07

### 🐛 Bug Fixes

#### חיסכון בסוללה עצר זיהוי Bluetooth/GPS ברקע בלי סימן אזהרה
- **אובחן**: יומני האבחון הראו ששירות הרקע פעיל, ההרשאות מאושרות, ו-Bluetooth "startWatch" הצליח — ועדיין אף אירוע חיבור/ניתוק Bluetooth או חציית סף GPS מעולם לא הגיע ליומן, וגם פעולות הווידג'טים תמיד נפלו חזרה לפתיחת האפליקציה. שירות הרקע ותהליך האפליקציה נשארים חיים, אבל זה **לא מבטיח** שה-Activity (וה-WebView שבו רץ כל קוד ה-Bluetooth/GPS) שורד ברקע — Android (או שכבת מותג אגרסיבית) יכולים "לגרוס" אותו כשחיסכון בסוללה פעיל, גם בלי שהמשתמש סגר את האפליקציה בכוח
- **התיקון**: האפליקציה מבקשת כעת באופן יזום פטור מחיסכון בסוללה (Battery Optimization) בפתיחה ראשונה, ומציגה אזהרה ייעודית עם כפתור לביטול ההגבלה בהגדרות Bluetooth אם היא עדיין פעילה — הרבה יותר אפקטיבי מלבקש מהמשתמש לחפש את זה בעצמו בהגדרות

### 🔧 Technical
- `BluetoothClassicPlugin.kt` — מתודות חדשות `batteryOptimizationStatus`/`requestIgnoreBatteryOptimizations` (עם הגנת גרסת API, כי minSdk הוא 22 והמתודות הרלוונטיות זמינות רק מ-API 23)
- הוערך ונדחה פתרון מעמיק יותר (WebView נפרד ועצמאי בתוך שירות הרקע) — מורכב ושביר מדי לשילוח בלי מכשיר לבדיקה; הפתרון שנבחר משתמש במנגנון הרשמי של אנדרואיד לבעיה הזו בדיוק
- תועד ב-CLAUDE.md: מגבלה ידועה חדשה — שירות רקע פעיל אינו ערובה ל-Activity/WebView חי; חיסכון בסוללה או הגבלות OEM ספציפיות (Xiaomi/Huawei/Samsung וכו', וגם "אל תשמור פעילויות" באפשרויות מפתחים) יכולים עדיין לעצור הכל בלי שום שגיאה גלויה

## [1.18.0] — 2026-09-07

### 🐛 Bug Fixes

#### תפריט הפעולות המהירות של הווידג'טים פתח את האפליקציה
- **תוקן**: לחיצה על "⋮" פתחה חלון קטן, אך כל כפתור בו (שמור/החלף/סיים חניה) הפעיל את הפעולה על ידי פתיחת האפליקציה במלואה — לא מה שהמשתמש ציפה מ"תפריט על הווידג'ט"
- **התיקון**: כל הפעולות רצות כעת ברקע (`evaluateJavascript` מול ה-WebView הפעיל של האפליקציה, בלי להביא אותה לקדמת המסך) ומציגות הודעה קצרה (Toast) עם התוצאה — ללא פתיחת האפליקציה. אם האפליקציה נסגרה לגמרי (לא רק ברקע), הפעולה חוזרת לנפילה חזרה הישנה של פתיחת האפליקציה

### ✨ New Features

#### בורר רכב בתפריט הווידג'טים
- תפריט "⋮" (זמין כעת בכל 3 הווידג'טים, לא רק "חניה פעילה") מציג בורר רכב — ברירת המחדל היא הרכב הפעיל, ואפשר לבחור רכב אחר ולבצע עליו את הפעולה ישירות
- ווידג'ט "שמירה מהירה" שומר חניה בלחיצה אחת ישירות, ללא פתיחת האפליקציה כלל
- "ניהול רכבים" בתפריט הוא הפעולה היחידה שעדיין פותחת את האפליקציה (הוספה/עריכה של רכב דורשת ממשק אמיתי)

### 🔧 Technical
- `FindMyCarApp.performWidgetAction(action, vehicleId)` — מתודה ציבורית חדשה (לא `#private`) שקוראים לה ישירות מקוד נייטיבי דרך `evaluateJavascript`
- `WidgetActionReceiver.kt` — `BroadcastReceiver` חדש; מפעיל את הפעולה מול ה-WebView החי של `MainActivity` (הפניה סטטית `WeakReference`), עם נפילה חזרה לפתיחת האפליקציה אם היא לא פעילה כלל
- `WidgetJsBridge.kt` — גשר `@JavascriptInterface` חדש (`window.AndroidWidgetBridge`) שמחזיר את תוצאת הפעולה בחזרה לצד הנייטיבי כ-Toast
- `WidgetDataPlugin.syncVehicles()` — מסנכרן רשימת רכבים + הרכב הפעיל ל-SharedPreferences כדי שהתפריט הנייטיבי יוכל להציג בורר רכב
- `release-checklist` עודכן לבדוק שהמנגנון החדש (הרשאות/רישום/חיווט) נשאר תקין בכל שחרור

## [1.17.0] — 2026-09-06

### ✨ New Features

#### יומן אבחון (Bluetooth / GPS / התראות)
- מסך הגדרות מקבל אזור "יומן אבחון" חדש — מציג רשומות מדויקות של כל אירוע Bluetooth (חיבור/ניתוק, כולל למה כל אחד טופל או התעלם ממנו), הצעת סיום חניה לפי GPS, ובקשת/תוצאת הרשאה, שהאפליקציה זיהתה בפועל
- הרשומות נשמרות 3 ימים, עם סינון לפי רכב ולפי קטגוריה, כפתור העתקה ללוח, ייצוא כקובץ טקסט (דרך אותו מנגנון שיתוף כמו גיבוי הנתונים), וניקוי
- נועד לענות על השאלה "האם האפליקציה בכלל קיבלה את האירוע?" בלי גישה למכשיר מחובר או ל-adb

#### בדיקת "שירות רקע פעיל"
- כ-1.5 שניות אחרי הפעלת מעקב Bluetooth, האפליקציה בודקת ישירות מול הצד הנייטיבי האם `ParkingForegroundService` (השירות שאחראי לשמור על זיהוי Bluetooth/GPS פעיל ברקע) אכן עלה בהצלחה, ורושמת את התוצאה ביומן האבחון — זו העובדה החשובה ביותר לאבחון "למה זיהוי ברקע לא עובד", ועד כה לא הייתה שום דרך לדעת אותה

### 🔧 Technical
- `js/diag-log.js` — מודול `DiagLog` חדש; רשומות נשמרות תחת מפתח שאינו מתחיל ב-`fmc_` כדי שלא ייכללו בגיבוי/שחזור נתוני המשתמש
- רישום נוסף בכל שכבות ה-Bluetooth (`js/bluetooth.js`, `js/bluetooth-native.js`), GPS, הרשאות (`#primeNativePermissions`) והתראות (`js/notify.js`)
- `BluetoothClassicPlugin.kt` — מתודה חדשה `isForegroundServiceRunning`; רישומי Logcat (`Log.d/i/w/e`) נוספו ל-`BluetoothClassicPlugin.kt` ול-`ParkingForegroundService.kt` (תגיות `FMC-BtPlugin`, `FMC-FgService`) לאבחון דרך `adb logcat` בנוסף ליומן שבתוך האפליקציה
- `release-checklist` עודכן לבדוק שהיומן ותצוגת הגרסה/"מה חדש" נשארים מחוברים בכל שחרור

## [1.16.0] — 2026-09-06

### 🐛 Bug Fixes

#### מחוון מכשיר Bluetooth מקושר לא השתנה חזותית
- **תוקן**: לאחר בחירת מכשיר Bluetooth בהגדרות רכב, השדה נשאר באותו צבע אפור/מושתק כמו לפני הבחירה — אין שום אינדיקציה חזותית שהמכשיר קושר בהצלחה. עכשיו השדה מקבל צבע מודגש (כחול המותג) ומשקל גופן מודגש כאשר מכשיר מקושר

#### נוטיפיקציות לא נראות בפועל
- **תוקן**: נוטיפיקציית הרקע הקבועה נשלחה בעדיפות `Min` — קטגוריה שמוסתרת תחת "הצג התראות שקטות" ודורשת הרחבה ידנית, מה שנראה כאילו האפליקציה לא שולחת שום התראה. הועלתה ל-`Low` (עדיין ללא צליל/קפיצה), ומוצגת עם אייקון ממותג במקום אייקון מערכת גנרי
- הוגדר אייקון ברירת מחדל ממותג גם להתראות הרקע החד-פעמיות (Bluetooth/GPS) — קודם השתמשו בנפילה חזרה לאייקון מערכת גנרי כשלא הוגדר

### ✨ New Features

#### אבחון הרשאות Bluetooth/נוטיפיקציות
- מסך הגדרות Bluetooth מציג כעת אזהרה אם הרשאת Bluetooth נחסמה לצמיתות על ידי המערכת (לאחר סירוב חוזר) או אם הרשאת נוטיפיקציות אינה מאושרת — מצב ששקוף לחלוטין למשתמש בעבר, כי האפליקציה פשוט ממשיכה לבקש הרשאה בלי שהמערכת מציגה עוד חלון, ונראה כאילו שום דבר לא קורה
- כפתור "פתח הגדרות אפליקציה" באזהרה זו, וגם בכישלון סריקת מכשירים, פותח ישירות את מסך ההרשאות של האפליקציה באנדרואיד
- לינוק מכשיר Bluetooth חדש לרכב מציג הודעה המפנה להגדרות Bluetooth כדי להפעיל בפועל את ההתחלה/סיום האוטומטיים (טוגלים נפרדים, כבויים כברירת מחדל)

#### ווידג'טים קטנים יותר + פעולות מהירות
- ווידג'טים "חניה פעילה" ו"שמירה מהירה" הוקטנו משמעותית (מידות רשת, גופן, ריפוד) — ווידג'ט "מפה מוקטנת" נשאר ללא שינוי
- ווידג'ט "חניה פעילה" מקבל כפתור "⋮" הפותח חלון פעולות מהירות (שמור חניה / החלף חניה / זזתי / בחר רכב) — מאחר שווידג'ט אמיתי לא יכול ליירט לחיצה ארוכה (שמורה למערכת ההפעלה לצורך הזזה/שינוי גודל/הסרה), זהו התחליף המעשי הקרוב ביותר לתפריט הקשר

## [1.15.0] — 2026-09-05

### 🐛 Bug Fixes

#### הרשאות לא נבדקות מחדש לאחר ייבוא גיבוי
- **תוקן**: ייבוא קובץ גיבוי (`#importData`) יכול להכניס הגדרות שההתקנה הנוכחית מעולם לא ביקשה הרשאה עבורן (למשל רכב עם Bluetooth אוטומטי או הצעת סיום חניה לפי GPS) — עכשיו הייבוא מריץ מחדש את אותו רצף בקשות ההרשאה (`#primeNativePermissions`) שרץ בפתיחה ראשונה, לפני הטעינה מחדש

### 🎨 Design

#### אייקון אפליקציה מותאם לאייקונים מסתגלים
- **תוקן**: שכבת החזית של האייקון המסתגל (Adaptive Icon) הייתה עותק שטוח של האייקון המלא כולל עיגול הרקע עד קצוות הקנבס, כך שמסכת המערכת (עיגול/מרובע מעוגל וכו') חתכה אותו באגרסיביות בכל לואנצ'ר. עכשיו שכבת החזית מכילה רק את צורת הרכב והפין, ממורכזת באזור הבטוח, עם שכבת רקע נפרדת בגרדיאנט המותג

#### עיצוב מחודש לווידג'טים
- **שופר**: ווידג'ט "חניה פעילה" — פריסה אופקית עם תג אייקון עגול לרכב במקום טקסט משורשר
- **שופר**: ווידג'ט "שמירה מהירה" — אייקון פין וקטורי במקום אמוג'י, רקע בגרדיאנט המותג וצל קל
- **שופר**: ווידג'ט "מפה מוקטנת" — פינות מעוגלות לתמונת המפה, סיכת מיקום בצורת טיפה עם צל רך, וכיתוב כתובת על גבי שכבת הצללה בתחתית התמונה

## [1.14.0] — 2026-09-05

### 🐛 Bug Fixes

#### הרשאות מצלמה/הקלטה חסרות באנדרואיד
- **תוקן**: `AndroidManifest.xml` לא הצהיר על `CAMERA`/`RECORD_AUDIO`/`MODIFY_AUDIO_SETTINGS` כלל — כך ש-`getUserMedia()` נכשל תמיד באפליקציית האנדרואיד, בלי קשר לבחירת המשתמש בחלון ההרשאה (הרשאה חסרה ב-manifest לא ניתנת להענקה בזמן ריצה בשום מצב). בפועל צילום תמונה והקלטה קולית תמיד נפלו למסך השגיאה/חלופת קובץ ומעולם לא הציגו תצוגה חיה

### ✨ New Features

#### בקשת הרשאות מרוכזת בפתיחה ראשונה (APK)
- באפליקציית האנדרואיד, מיד עם הפתיחה הראשונה מוצגות בקשות הרשאה למיקום, Bluetooth, מצלמה, מיקרופון ונוטיפיקציות — במקום הפתעה עם חלונות הרשאה מפוזרים בפעם הראשונה שכל תכונה נוגעת
- הפעלת המתג הראשי של Bluetooth בהגדרות בודקת שוב ומבקשת הרשאה אם עדיין חסרה (למשל אם נדחתה בפתיחה הראשונה)

---

## [1.13.2] — 2026-09-05

### 🐛 Bug Fixes

#### גלילת מסך הרכבים/הגדרות
- **תוקן**: מסך "הרכבים שלי" גלל רק את רשימת הרכבים בתוך אזור מצומצם משלה, בעוד הכפתורים מתחת (Bluetooth, GPS, טעינת גרסה, גיבוי ושחזור) תפסו מקום קבוע — עם הוספת אזור הגיבוי/שחזור זה כיווץ את רשימת הרכבים לפס דחוס כמעט בלתי-שמיש
- **התיקון**: כל המסך גולל כעת כיחידה אחת — הרכבים וכל שאר האזורים

#### שדרוג APK בלי הסרת התקנה
- **תוקן**: כל build ב-CI היה חתום במפתח debug אקראי שנוצר מחדש בכל ריצה (כי ה-runner הוא מכונה חדשה ללא `~/.android/debug.keystore` קודם) — אנדרואיד מסרב להתקין APK עם חתימה שונה מעל התקנה קיימת, ולכן היה צורך להסיר את ההתקנה הקודמת לפני כל עדכון
- **התיקון**: נוצר keystore debug קבוע (`android/app/debug.keystore`, לא סוד — משמש רק ל-debug build) שנשמר בריפו ומוגדר מפורשות ב-`build.gradle`, כך שכל ה-builds חתומים באותו מפתח וניתן להתקין עדכון ישירות מעל הגרסה הקיימת ללא הסרה

---

## [1.13.1] — 2026-09-05

### 🐛 Bug Fixes

#### קריסה בפתיחה ראשונה של אפליקציית האנדרואיד (Android 14)
- **תוקן**: על מכשירי Android 14, האפליקציה קרסה מיד בפתיחה עם `SecurityException` — שירות הרקע (`ParkingForegroundService`) היה מוצהר עם `foregroundServiceType="connectedDevice"` בלבד, שדורש הרשאת `BLUETOOTH_CONNECT` **מאושרת בפועל** (לא רק מוצהרת ב-manifest) לפני שאפשר להפעיל אותו — וההרשאה מתבקשת רק בהמשך, כשמקשרים מכשיר Bluetooth לרכב. בפועל זה קרס בכל שמירת חניה ראשונה, לא רק בזרימת ה-Bluetooth
- **התיקון**: השירות בוחר עכשיו דינמית את הסוג בזמן ריצה — `connectedDevice` אם ההרשאה כבר אושרה, אחרת `specialUse` (סוג ללא דרישת הרשאה מיוחדת) — כך שההפעלה הראשונה אף פעם לא תלויה בהרשאת Bluetooth
- הוספה גם רשת ביטחון: כל כשל בהפעלת/עצירת השירות נתפס ולא יכול עוד להפיל את כל האפליקציה

---

## [1.13.0] — 2026-09-05

### ✨ New Features

#### הצעת סיום חניה גם לפי מרחק
- בנוסף לבדיקת המהירות הקיימת, האפליקציה בודקת גם את המרחק מהמיקום השמור של החניה — מעל 300 מ' מציגה את אותה הצעת "לסיים את החניה?" (לא סוגרת אוטומטית, כדי לא להיטעות מהליכה רגלית — תמיד דורשת אישור)
- מכסה מקרים בהם מהירות ה-GPS אינה זמינה/אמינה (למשל נסיעה איטית בפקק)

#### נוטיפיקציות מערכת ל-Bluetooth/GPS ברקע (APK)
- כשהמסך כבוי או האפליקציה ברקע, אירועי Bluetooth (חיבור→הצעת/ביצוע סיום חניה, ניתוק→התחלת חניה אוטומטית) והצעת סיום לפי GPS מציגים כעת גם נוטיפיקציית מערכת — לא רק הודעה בתוך האפליקציה שאי אפשר לראות כשהיא ברקע
- באפליקציה בלבד: כתובת ה-Toast/Modal הקיימים נשארים ללא שינוי כשהאפליקציה בחזית; הנוטיפיקציה מוצגת רק כשהאפליקציה ברקע, כדי לא לכפול התראות

### 🔧 Technical
- `js/notify.js` — `Notify.show()` חדש: באפליקציה הנייטיבית משתמש ב-`@capacitor/local-notifications` (הפתרון הרשמי היציב להתראות בתוך WebView — ה-Web Notifications הגולמי ידוע כלא אמין בתוך WebView טהור); בדפדפן ממשיך להשתמש ב-`ServiceWorkerRegistration.showNotification()` הקיים
- `#suggestGpsEnd()` — חילוץ לוגיקה משותפת בין בדיקת המהירות והמרחק, כדי שלא תיפתח הצעה כפולה מאותו עדכון מיקום
- `CFG.gpsDistanceThreshold` (300 מ') — סף המרחק החדש, נבדק דרך אותו toggle קיים של "הצעת סיום לפי GPS" בהגדרות

---

## [1.12.0] — 2026-09-05

### ✨ New Features

#### גיבוי ושחזור נתונים (PWA ↔ APK)
- מסך ההגדרות מקבל אזור "גיבוי ושחזור נתונים" עם כפתורי **ייצוא** ו**ייבוא**
- **ייצוא**: יוצר קובץ JSON עם כל הרכבים, ההיסטוריה וההגדרות. בדפדפן — הורדה רגילה; באפליקציית האנדרואיד — נשמר זמנית ונפתח דרך חלון השיתוף המובנה (Share) כדי לשמור/לשלוח לכל יעד
- **ייבוא**: בחירת קובץ גיבוי (עם אישור לפני החלפת נתונים קיימים) וטעינה מחדש של האפליקציה

### 🔧 Technical
- ה-PWA וה-APK רצים על origins נפרדים (localStorage אינו משותף) — אין דרך לסנכרן אוטומטית בלי שרת; קובץ הגיבוי הוא הדרך היחידה להעביר נתונים בין השניים באותו מכשיר
- `Store.exportAll()`/`Store.importAll()` — סורקים גנרית את כל המפתחות עם קידומת `fmc_`, כך שמפתחות עתידיים נכללים אוטומטית
- באפליקציה הנייטיבית: `@capacitor/filesystem` (כתיבה לתיקיית Cache הפרטית של האפליקציה, ללא צורך בהרשאות) + `@capacitor/share` (חלון שיתוף מובנה) — ללא שינוי קוד Kotlin ידני, נרשמים אוטומטית דרך `cap sync`

---

## [1.11.0] — 2026-09-04

### ✨ New Features

#### אפליקציית Android (APK) — ערוץ הפצה נוסף
- נוסף פרויקט Android native (Capacitor) שעוטף את אותו קוד PWA בדיוק — אין הבדל ב-UI/פיצ'רים בין שתי הגרסאות
- **זיהוי Bluetooth אמיתי ברקע**: במקום הפרוקסי של הדפדפן (`enumerateDevices`/`devicechange`, שעובד רק כשהטאב פעיל בקדמה), האפליקציה הנייטיבית מאזינה ישירות לשידורי המערכת `ACL_CONNECTED`/`ACL_DISCONNECTED` — כך שסיום/התחלת חניה אוטומטיים לפי Bluetooth עובדים גם כשהמסך כבוי או האפליקציה ברקע
- שירות קדמה (foreground service) עם התראה קבועה שומר את התהליך פעיל בזמן חניה או כשזיהוי Bluetooth מופעל — כך שגם זיהוי מהירות GPS ממשיך לפעול ברקע
- **שלושה ווידג'טים למסך הבית**: "חניה פעילה" (כתובת + זמן), "שמירה מהירה" (כפתור לשמירת חניה בלחיצה אחת), "מפה מוקטנת" (תמונת מפה סטטית של מיקום החניה)
- GitHub Action חדש (`build-android.yml`) מקמפל APK להתקנה בכל push ל-`main`, וניתן להריץ ידנית

### 🔧 Technical
- `js/bluetooth-native.js` — מימוש חלופי ל-`BluetoothController` עם אותו interface בדיוק, מגובה ע"י plugin נייטיבי; הבחירה בין הגרסה הדפדפנית לנייטיבית ב-`js/app.js` דרך `Capacitor.isNativePlatform()`
- `js/widget-bridge.js` — מסנכרן את מצב החניה הנוכחי לווידג'טים; ללא פעולה בדפדפן
- פרויקט `android/` חדש (Kotlin): `BluetoothClassicPlugin`, `ParkingForegroundService`, `WidgetDataPlugin`, ו-3 `AppWidgetProvider`
- `capacitor.config.json` + `scripts/prepare-www.mjs` (מעתיק את קבצי ה-PWA ל-`www/` הזמני לפני `cap sync` — ה-root נשאר מקור האמת היחיד)

---

## [1.10.0] — 2026-07-09

### ✨ New Features

#### "החלף חניה" במסך הכניסה
- **כפתור "החלף חניה"** נוסף למסך הקופץ "ברוך השב!" שמוצג בחזרה לאפליקציה עם חניה פעילה
- הכפתור מאפשר להחליף את מיקום החניה ישירות מהמסך הקופץ — סוגר את המסך, שומר את החניה הנוכחית להיסטוריה ומיד קולט GPS ושומר חניה חדשה

### 🔧 Technical
- `ReturnModal` מקבל כעת callback שלישי `onSwap`, מחובר לאירוע `returnSwapBtn`

---

## [1.9.0] — 2026-07-09

### ✨ New Features

#### החלף חניה — החלפה אוטומטית
- **כפתור "החלף חניה"** מוצג בכרטיס החניה הפעילה
- לחיצה על הכפתור שומרת את החניה הנוכחית להיסטוריה ומיד מתחילה חניה חדשה במיקום ה-GPS הנוכחי — הכל בפעולה אחת
- ה-Wake Lock, הטיימר וההתראה מתעדכנים אוטומטית עבור החניה החדשה
- גיאוקודינג (הצגת כתובת) מתבצע ברקע לאחר החלפה

### 🔧 Technical
- `#swapParking()` — שיטה פרטית חדשה ב-`FindMyCarApp` המבצעת העברה להיסטוריה + שמירת חניה חדשה אטומית

---

## [1.8.0] — 2026-07-01

### ✨ New Features

#### Wake Lock — מסך פעיל בזמן חניה
- **Screen Wake Lock** — כאשר חניה פעילה, האפליקציה מבקשת מהמכשיר לשמור את המסך דולק (`navigator.wakeLock`), כך שה-GPS וה-Bluetooth ממשיכים לעבוד
- משוחרר אוטומטית כשהחניה מסתיימת; מוחזר אוטומטית בחזרה לאפליקציה לאחר נעילת מסך

#### נוטיפיקציה — חניה פעילה ברקע
- **התראה מתמשכת** — כאשר חניה מתחילה, מוצגת נוטיפיקציה עם כתובת הרכב
- הנוטיפיקציה מתעדכנת עם הכתובת הסופית לאחר ה-Geocoding
- **לחיצה על ההתראה** — פותחת / מחזירה פוקוס לאפליקציה (מטופל ב-Service Worker)
- ההתראה נסגרת אוטומטית עם סיום החניה

#### זיהוי Bluetooth מחדש בחזרה לאפליקציה
- בכל חזרה לחלונית הפעילה (`visibilitychange`), מתבצעת סריקת מכשירי Bluetooth מחדש
- מזהה חיבורים/ניתוקים שקרו בזמן שהאפליקציה הייתה ברקע

### 🔧 Technical
- `BluetoothController.checkNow()` — שיטה ציבורית חדשה לסריקת שינויי מכשירים לפי דרישה
- `#acquireWakeLock()`, `#releaseWakeLock()` — ניהול Wake Lock ב-`FindMyCarApp`
- `#showParkingNotification(parking)`, `#cancelParkingNotification()` — ניהול התראת חניה
- `sw.js` מטפל בלחיצה על התראה (`notificationclick`)
- `sw.js` עודכן ל-`findmycar-v1.8.0`
- `CFG.keys.notifTag = 'fmc-parking-active'`

---

## [1.7.0] — 2026-07-01

### ✨ New Features

#### כפתור סיום חניה ישיר
- **סיום חניה בלחיצה אחת** — כפתור "סיום חניה" ישיר נוסף בכרטיס החניה הפעילה, מתחת לכפתורי הפעולות הקיימים
- ללחיצה אחת — ללא צורך בדיאלוג אישור נוסף

#### הצעת סיום חניה לפי מהירות GPS
- **זיהוי נסיעה ברכב** — כאשר מהירות GPS עולה על ~25 קמ"ש לאורך 8 שניות רצופות, מוצגת הצעה לסיים את החניה
- **הגדרה בלשונית הרכבים** — ניתן להפעיל/לכבות את התכונה בלחיצה אחת
- **ברירת מחדל: כבוי** — לא פוגע בחוויה הקיימת

### 🔧 Bug Fixes (v1.6.1 → v1.7.0)
- תיקון: חיבור Bluetooth שני לא מחליף את הוודאות הפתוחה של הרכב הראשון
- תיקון: `btStartDevice` עלול להכתב על החניה הלא נכונה אחרי החלפת רכב בזמן שמירת GPS
- תיקון: תווית "הסתיים אוטומטית" הוחלפה ב-"הסתיים ב-Bluetooth" (מדויק גם לאישור ידני)
- תיקון: `#markBtEnd` משתמש כעת ב-`this.#state.current` כמקור ראשי לפי הדפוס הסטנדרטי

### 🔧 Technical
- שדה `gpsAutoEnd` חדש ב-localStorage: `fmc_gps_auto_end_v1`
- קבועים: `CFG.gpsSpeedThreshold = 7` (m/s), `CFG.gpsSpeedDuration = 8000` (ms)
- שדות state חדשים: `gpsSpeedSince`, `gpsEndSuggested`
- `sw.js` עודכן ל-`findmycar-v1.7.0`

---

## [1.6.1] — 2026-07-01

### ✨ New Features

#### Bluetooth Metadata in Parking History
- **מידע BT בהיסטוריה** — כל חניה שהתחילה או הסתיימה דרך Bluetooth מוצגת עם תג 🔵 BT ברשימת ההיסטוריה
- **פירוט בחלון פרטים** — חלון פרטי החניה מציג:
  - שם התקן ה-BT שגרם להתחלה האוטומטית
  - שם התקן ה-BT שגרם לסיום האוטומטי, כולל חותמת זמן
- **שדות חדשים בפרקינג**: `btStartDevice`, `btEndDevice`, `btEndTime`

### 🔧 Technical
- שדות BT חדשים נשמרים בכל אובייקט חניה החל מ-v1.6.1
- שדות ישנים שנשמרו ב-v1.6.0 יוצגו ללא תגי BT (תאימות לאחור)
- `sw.js` עודכן ל-`findmycar-v1.6.1`

---

## [1.6.0] — 2026-06-30

### ✨ New Features

#### Bluetooth Device Linking
- **קישור Bluetooth לרכב** — ניתן לקשר התקן Bluetooth (לדוגמה מערכת שמע של הרכב) לכל רכב בנפרד, ישירות ממסך עריכת הרכב
- **זיהוי חיבור** — כאשר מתחברים ל-Bluetooth שקושר לרכב עם חניה פעילה, האפליקציה מציעה לסיים את החניה
- **סיום אוטומטי** — ניתן להגדיר שבחיבור ל-Bluetooth הסיום יתבצע אוטומטית ללא שאילתה

#### Bluetooth Auto-Start Parking on Disconnect
- **התחלת חניה אוטומטית** — בניתוק מ-Bluetooth של הרכב, האפליקציה שומרת חניה חדשה לפי GPS באופן אוטומטי
- **חלון הוספת מדיה** — לאחר שמירה אוטומטית, חלון popup מציע להוסיף תמונה, הקלטה קולית או תיאור טקסטואלי

#### Bluetooth Settings Screen
- **מסך הגדרות Bluetooth** — כפתור ייעודי בלשונית הרכבים פותח מסך הגדרות מלא
- **הגדרות לכלל הרכבים** — שלושה מתגים שמגדירים ברירת מחדל לכל הרכבים בו-זמנית:
  - סיום חניה אוטומטי בחיבור
  - התחלת חניה אוטומטית בניתוק
  - הצגת חלון הוספת מדיה לאחר שמירה
- **הגדרות לכל רכב בנפרד** — כל רכב עם Bluetooth מקושר מציג מתגים עצמאיים לאותן הגדרות
- **מתג ראשי** — ניתן לכבות את כל פיצ'ר ה-Bluetooth באחת

### 🔧 Technical
- מודול חדש `js/bluetooth.js` — `BluetoothController` עם מעקב `devicechange`
- זיהוי חיבור וניתוק של מכשירי שמע (audiooutput + audioinput) באמצעות `enumerateDevices()`
- שמירת הגדרות Bluetooth ב-`fmc_bluetooth_v1` (localStorage)
- שדות Bluetooth חדשים ב-Vehicle: `bluetoothDevice`, `bluetoothAutoEnd`, `bluetoothAutoStart`, `bluetoothStartPopup`

---

## [1.5.0] — 2026-06-27

### ✨ New Features

#### Inline Media Display
- **תמונה inline** — תמונה שמורה מוצגת ישירות בכרטיס החניה; לחיצה פותחת תצוגה מלאה
- **הקלטה inline** — נגן אודיו מוטמע ישירות בכרטיס, ניתן להאזין ללא פתיחת חלון
- **תיאור inline** — הטקסט השמור מוצג בכרטיס עם כפתורי עריכה ומחיקה

#### Individual Media Delete
- כפתור 🗑️ על כל פריט מדיה (תמונה, הקלטה, תיאור) למחיקה עצמאית
- אישור מחיקה לתמונה ולהקלטה; מחיקת תיאור מיידית
- לחצן ✏️ לעריכת התיאור ישירות מהכרטיס

---

## [1.4.0] — 2026-06-26

### ✨ New Features

#### Active Parking Chip Shortcut
- **Tap "חניה פעילה"** — Clicking the active parking status chip in the header now opens the end-parking confirmation modal directly

#### Vehicle Details
- **License plate** — Optional license plate number field in vehicle settings (LTR input, max 15 chars)
- **Color** — Optional vehicle color field in vehicle settings (max 20 chars)
- Both fields appear as a secondary info line below the vehicle name in the vehicles list

---

## [1.3.0] — 2026-06-26

### ✨ New Features

#### Version Display & What's New
- **Version tag** — Version number (e.g. `v1.3.0`) shown as a tappable pill in the header
- **What's New modal** — Automatically shown once per upgrade; also accessible by tapping the version tag
- First-install users see no popup (only returning users on upgrade)

#### Map Improvements
- **Map fix** — Removed SRI `integrity` attributes from Leaflet CDN tags that caused load failures in proxy/CDN environments; map now reliably loads tiles
- **Map collapse/expand** — Tap the bar at the bottom of the map to hide or show it; state is persisted across sessions
- **invalidateSize on reveal** — Map tiles recalculate layout correctly after loading screen fade and after expand
- **Internal invalidateSize** — Map self-recalculates after 400ms to handle any container size changes

#### Vehicle Parking Management
- **Clear parking per vehicle** — 🧹 button in the Vehicles settings view moves a vehicle's current parking to history without switching vehicles

#### Load New Version Button
- **"טען גרסה חדשה" button** — In the Vehicles settings view; clears all caches, unregisters the service worker, and reloads to pick up the latest deployed version
- **Auto-reload on SW update** — App reloads automatically when a new service worker takes control

### 🐛 Bug Fixes
- Deleted active vehicle no longer stays visible in the settings list after deletion
- What's New modal no longer conflicts with the return-to-car modal on upgrade
- What's New now correctly shows for users upgrading from v1.2.0 (which never stored the seen-version key)

### 🏗️ Architecture
- `js/config.js` — `seenVersion` and `mapCollapsed` storage keys; `changelog` array (deeply frozen)
- `js/app.js` — `#toggleMapCollapse`, `#setMapCollapsed`, `#clearVehicleParking`, `#settingsCbs` helpers
- `js/ui.js` — `showWhatsNew(entry)` method; `renderSettingsView` now accepts `onClearParking` / `hasParking` callbacks
- `style.css` — version-tag, map-toggle-bar/btn/chevron, vehicle-parking-indicator, vehicle-clear-btn, whats-new-list; map overlay color uses `--map-overlay-bg` CSS variable

### 🔧 Service Worker
- `CACHE_NAME` bumped to `findmycar-v1.3.0`

---

## [1.2.0] — 2026-06-26

### ✨ New Features

#### Multi-Vehicle Support
- **Vehicle management** — Add, edit, and delete vehicles from a dedicated "Vehicles" settings screen (bottom nav)
- **Vehicle selector** — Horizontal chip row in the home view (shown when 2+ vehicles are configured)
- **Per-vehicle parking data** — Each vehicle maintains its own active parking and history
- **Storage migration** — On first load, existing data is automatically migrated to the default vehicle (`הרכב שלי`)
- Maximum 5 vehicles; up to 30 characters per vehicle name; 8 icon choices
- Return-to-car modal now shows the specific vehicle's name

#### WhatsApp Sharing
- **WhatsApp button** in the parking action grid
- **Content selection modal** — Choose what to include: vehicle name, address, time, description, map link, and photo
- **Smart file sharing** — If a photo is selected, uses the native `navigator.share({ files })` API for the system share sheet; falls back to `wa.me/?text=…` for text-only or when the File Share API is unavailable

#### Tests
- **33 unit tests** (Vitest + jsdom):
  - `tests/unit/utils.test.js` — UUID, escHtml, Haversine distance, dataUrlToFile
  - `tests/unit/store.test.js` — localStorage CRUD
  - `tests/unit/vehicles.test.js` — VehicleController full lifecycle
- **E2E tests** (Playwright + Chromium): `tests/e2e/app.spec.js` — smoke tests for all views and key interactions
- `package.json` with Vitest + Playwright devDependencies

### 🏗️ Architecture
- New `js/vehicles.js` module — `VehicleController` with CRUD + localStorage migration
- `js/config.js` — vehicle keys, `maxVehicles`, `maxVehicleNameLen`, `vehicleIcons`
- `js/utils.js` — added `dataUrlToFile()` for Web Share API file conversion
- `js/app.js` — vehicle state management, `#currentKey`/`#historyKey` computed getters, vehicle lifecycle methods, WhatsApp sharing
- `js/ui.js` — `renderVehicleSelector()`, `renderSettingsView()`, `populateVehicleModal()`, `getVehicleModalValues()`, `populateWhatsAppModal()`, `getWhatsAppOptions()`
- `js/return-modal.js` — `show(parking, vehicleName)` for vehicle-specific subtitle

### 🔧 Service Worker
- `CACHE_NAME` bumped to `findmycar-v1.2.0`
- `./js/vehicles.js` added to `STATIC_ASSETS`

---

## [1.1.0] — 2026-06-25

### ✨ New Features

#### Enhanced Address Detection
- **Structured geocoding** — Nominatim response parsed into `{ display, street, houseNumber, city, neighborhood }`
- **Two-line address display** — Street + house number prominent; city/neighborhood on a secondary line
- **Backward compatible** — Existing string addresses render correctly via `normalizeAddress()`

#### Return-to-Car Modal
- **Auto-shown on app entry** — Appears after loading screen if an active parking session exists
- Shows elapsed time (highlighted badge), address, photo thumbnail, description, voice indicator
- **"זזתי" button** — Archives current parking to history and clears it
- **"המשך לנווט" button** — Dismisses modal, parking session stays active

### 🏗️ Architecture — ES Module Split

Monolithic `app.js` refactored into focused ES modules under `js/`:

| Module | Responsibility |
|--------|---------------|
| `js/config.js` | Frozen `CFG` constants |
| `js/store.js` | `localStorage` wrapper |
| `js/utils.js` | Pure utilities (uuid, formatters, Haversine, image compression) |
| `js/geocoder.js` | `reverseGeocode()` → structured `AddressObj`; `normalizeAddress()` |
| `js/map.js` | `MapController` — Leaflet map + detail mini-map lifecycle |
| `js/camera.js` | `CameraController` — getUserMedia, capture, file fallback |
| `js/voice.js` | `VoiceController` — MediaRecorder, blob URL lifecycle |
| `js/ui.js` | `UIController` — DOM rendering, modals, toasts, theme |
| `js/return-modal.js` | `ReturnModal` — auto-show return-to-car flow |
| `js/app.js` | `FindMyCarApp` orchestrator — state, events, parking lifecycle |

- `<script type="module">` replaces the classic `<script>` tag
- `<link rel="modulepreload">` for all modules
- Private class fields (`#`) used throughout for encapsulation

### 🔧 Service Worker
- `CACHE_NAME` bumped to `findmycar-v1.1.0`
- `STATIC_ASSETS` updated with all `js/*.js` module paths

---

## [1.0.0] — 2026-06-25

### ✨ Initial Release

#### Core Features
- **GPS Location Saving** — One-tap save of precise GPS coordinates with automatic reverse geocoding (Hebrew address via Nominatim)
- **Interactive Map** — Leaflet.js + OpenStreetMap, shows parking marker and user's current position
- **Parking Timer** — Live elapsed time counter showing how long the car has been parked
- **Distance Calculator** — Real-time distance from user's current location to parked car (Haversine formula)

#### Media Capture
- **Photo Capture** — Camera access via `getUserMedia` with front/rear camera toggle; file picker fallback for iOS
- **Voice Recording** — `MediaRecorder` API with visual pulse animation and 5-minute limit; file picker fallback
- **Text Description** — Free-text note up to 300 characters

#### Navigation & Sharing
- **Multi-app Navigation** — Open parking location in Waze, Google Maps, or Apple Maps
- **Share Location** — Web Share API with clipboard fallback; shares address, description, time, and map link

#### History & Storage
- **Parking History** — Stores up to 30 past parking sessions with all media
- **Detail View** — Tap any history item for full details including photo, audio playback, mini-map
- **localStorage persistence** — All data stored locally, never leaves the device
- **Reset / New Parking** — Current parking archived to history before starting new session

#### PWA & UX
- **Service Worker** — Offline support with cache-first for app shell, stale-while-revalidate for map tiles
- **Installable** — `manifest.json` with icons for Android (192, 512px) and iOS (180px)
- **Install Banner** — Prompts PWA install after 3 seconds on first visit
- **Dark / Light Theme** — Toggle with instant switch, persisted in localStorage
- **RTL Layout** — Full Hebrew right-to-left support throughout
- **Safe Area Support** — Respects iPhone notch / home indicator
- **Offline Indicator** — Shows banner when map tiles fail to load

#### Design
- Dark-first design with `#060B18` background
- CSS variables for complete theming
- Glassmorphism cards with subtle borders
- Smooth 250ms animations throughout
- Bottom sheet modals with drag handle
- Touch-optimized (44px minimum tap targets)
- Heebo font for Hebrew typography

---

## Planned for v1.1.0

- [ ] Parking lot / street name disambiguation
- [ ] Multiple vehicle support
- [ ] WhatsApp direct share shortcut
- [ ] Compass bearing to parking from current position
- [ ] Export history as JSON
- [ ] Reminder notification when parked too long
- [ ] Parking cost tracker (hourly rate input)
